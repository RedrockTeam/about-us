import { copywritingData } from './copywriting'
import { destinationData as destination } from './destination'
import { products } from './products'

export const publicData = Object.assign({}, copywritingData, destination, products)